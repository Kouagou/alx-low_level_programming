#include "main.h"
#include <string.h>

/**
 * _strlen - A function that returns the length of a string.
 * @s: The string to be proeeded
 *
 * Return: return the length of the string s
 */
int _strlen(char *s)
{
	return (strlen(s));
}
