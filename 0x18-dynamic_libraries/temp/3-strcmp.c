#include "main.h"
#include <string.h>

/**
 * _strcmp - A function that concatenates two strings
 * @s1: the string to which s2 will compared
 * @s2: the string to which s1 will compared
 * Return: return 0 if the s1 and s2 are equal,
 * a negative value if s1 is less than s2,
 * a positive value if s1 is greater than s2
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
