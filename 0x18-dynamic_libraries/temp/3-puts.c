#include "main.h"
#include <stdio.h>

/**
 * _puts - A function that prints a string,
 * followed by a new line, to stdout.
 * @str: The string to be proceeded
 *
 */
void _puts(char *str)
{
	puts(str);
}
